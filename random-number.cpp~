#include <iostream>
#include <cmath>
#include <ctime>


  const int a = 16807;
  const long m = pow(2,32)-1;
  const int c = 12345;
  long value = 1;
  
long random_number()
{
  value = ((a*value) + c) % m;
  return value;
}

int main()
{
  std::cout << time() << std::endl;
  return 0;
}
